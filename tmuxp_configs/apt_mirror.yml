# apt_mirror.yml
session_name: apt_mirror
windows:
- window_name: htop
  panes:
    - htop
- window_name: access_logs
  panes:
    - sudo su -c "lnav /var/log/apache2/access*.log"
- window_name: error_log
  panes:
    - sudo su -c "lnav /var/log/apache2/error*.log"
