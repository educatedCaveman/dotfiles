# nextcloud.yml
name: nextcloud
windows:
  - htop:     htop
  - status:   echo "sudo snap logs -f nextcloud"; sudo snap logs -f nextcloud
  - services: echo "sudo snap services nextcloud"; sudo snap services nextcloud
  - auth.log: lnav /var/log/auth.log*
  - syslog:   lnav /var/log/syslog*
  - ~
